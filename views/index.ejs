<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Climate Solutions – Home</title>
  <!-- TailwindCSS + DaisyUI (via CDN for development convenience) -->
  <link href="https://cdn.jsdelivr.net/npm/daisyui@5" rel="stylesheet" type="text/css" />
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-gray-50 font-sans">
  <!-- Include Navbar -->
  <%- include('partials/navbar', { page: 'home', user: user }) %>

  <div class="container mx-auto p-6">
    <h1 class="text-4xl font-bold text-center text-gray-800 mb-4">Welcome to Climate Solutions</h1>
    <p class="text-center text-gray-600 mb-8">Explore projects tackling climate change.</p>
    <div class="flex justify-center mb-10">
      <a href="/solutions/projects" class="btn btn-primary">Get Started Now</a>
    </div>

    <!-- Featured Projects (display first 3 projects as featured cards) -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      <% projects.slice(0, 3).forEach(project => { %>
        <div class="bg-white p-6 rounded-lg shadow-md">
          <!-- Project Image -->
          <img src="<%= project.feature_img_url %>" alt="<%= project.title %>" class="w-full h-48 object-cover rounded-t-lg mb-4">
          <!-- Project Title & Summary -->
          <h2 class="text-xl font-bold mb-2"><%= project.title %></h2>
          <p class="text-gray-700 mb-4"><%= project.summary_short %></p>
          <!-- Link to View Project Details -->
          <a href="/solutions/projects/<%= project.id %>" class="text-blue-500 hover:underline">View Project</a>
        </div>
      <% }); %>
    </div>
  </div>
</body>
</html>
